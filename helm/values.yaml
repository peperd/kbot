# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
autoscaling:
  enabled: false

image:
  repository: peperd
  tag: "v1.0.0-4445444-arm64"
  arch: amd64
secret:  
  tokenName: TELE_TOKEN
  tokenKey: token
  name: kbot